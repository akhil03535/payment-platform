apiVersion: 1

providers:
  - name:            PayFlow Dashboards
    orgId:           1
    folder:          PayFlow
    type:            file
    disableDeletion: false
    editable:        true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
